# Python-generated files
__pycache__/
*.py[cod]
*$py.class
build/
dist/
wheels/
*.egg-info

# ====================================================================
# 🧠 로컬 데이터베이스 및 임베디드 캐시 (무조건 제외 필수)
# ====================================================================
# Qdrant 로컬 파일 스토리지 (내부 이진 파일 및 락 파일 포함)
qdrant_local/

# SQLite 메타데이터 데이터베이스 파일
pipeline_metadata.db

# SQLite 임시 저널 및 로그 파일
*.db-journal
*.db-shm
*.db-wal

# HarvestFlow 런타임 로그 (일 단위 회전 백업 포함)
logs/
harvest.log
harvest.log.*

# ====================================================================
# 📁 옵시디언 원천 보관소 (로컬 가상 자산)
# ====================================================================
# 개인적인 메모나 원본 글이 포함되어 있으므로 제외 (선택 사항이나 보안상 권장)
# 만약 샘플 노트 몇 개만 깃허브에 공유하고 싶다면 이 줄을 주석 처리하세요.
obsidian_vault/

# 옵시디언 시스템 내부 설정 폴더 (.obsidian)
.obsidian/

# ====================================================================
# 🌐 Quartz 관련 자동 생성 및 빌드 캐시 파일 (이것들만 제외!)
# ====================================================================
# Quartz가 마크다운을 HTML 웹페이지로 최종 빌드해내는 아웃풋 폴더
quartz/public/

# Quartz 내부 빌드 과정에서 생성되는 임시 캐시 폴더들
.quartz-cache/
.next/
node_modules/
__quartz_runtime/
quartz-build.log

# ====================================================================
# 🐍 파이썬 환경 및 종속성 관리
# ====================================================================
# uv 또는 가상환경 폴더
.venv/
venv/
ENV/

# ====================================================================
# 🖥️ 운영체제 및 에디터 임시 파일
# ====================================================================
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로컬 비밀 설정
.env

# 스모크 테스트 임시 산출물
smoke_*/
smoke_pipeline.db
qdrant_smoke*/
quartz_smoke*/
.tmp_*

# 기타 작업 스크립트 (로컬 전용 도구 + 그 전용 의존성 파일)
render_readme_pdf.py
render_readme_pdf.requirements.txt