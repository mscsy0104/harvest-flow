# Sample Publish Output

This directory is the default publish target in OSS mode.

- Pipeline output markdown is written here when publishing is enabled.
- In personal mode, you can map this to `quartz_content` with an override file.

