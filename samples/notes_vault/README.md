# Sample Notes Vault

This directory is a safe demo vault for open-source users.

- Add markdown files to stage folders (for example `1_초안`).
- The default Docker compose mounts this path as `/app/notes_vault`.
- For personal usage, override `APP_VAULT_STEM` or use a compose override file.

